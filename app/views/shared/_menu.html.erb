<%= button_tag 'Edit the account', data: { type: 'dashboard-button', url: edit_account_path(account.id) }, remote: true unless diabetic %>
<%= button_tag "Add a record for: #{diabetic.name}", data: { type: 'dashboard-button', url: new_diabetic_record_path(diabetic) }, remote: true  if diabetic %>
