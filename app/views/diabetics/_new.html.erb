<%= form_for ([account,diabetic]), remote: true do |f| %>
  <%= f.text_field :name, :placeholder => "e.g. John Smith" %>
  <%= date_select :diabetic, :birthday, :start_year => Time.now.year - 120, :end_year => Time.now.year %>
  <%= f.text_field :email, :placeholder => "e.g. john@smith.com" %>
<% end %>
<script>
var title = 'Add a diabetic'
var leftButton = {
  type: 'cancel',
  text: 'Cancel'
}
var rightButton = {
  type: 'submit',
  text: 'Create'
}
</script>
