      # expect(response.body).to have_content(doctor.name)
      # expect(response.body).to have_content(doctor.fax)
      # expect(response.body).to have_content(doctor.email)
      # expect(response.body).to have_content(doctor.comments)